/**
 * @abiros/shared — types, zod schemas, and constants shared by API and web.
 * Keep this package free of runtime dependencies on Node or the DOM so it can
 * be imported from both sides.
 */
export * from './constants.js';
export * from './http.js';
